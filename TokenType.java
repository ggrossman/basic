
enum TokenType {
  IDENTIFIER,
  OPERATOR,
  NUMBER,
  STRING,
  RUN,
  LIST,
  PRINT, // New token type for the "print" reserved word
  INPUT, // New token type for the "input" reserved word
  LET, // New token type for the "let" reserved word
  REM, // New token type for the "rem" reserved word
  GOTO,
  IF,
  THEN,
  LOAD,
  SAVE
}
